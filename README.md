# 🎥 YouTube RAG Chatbot

A simple Retrieval-Augmented Generation (RAG) chatbot that answers questions from YouTube video transcripts.

## Features
- Extracts transcript from YouTube videos
- Uses embeddings + FAISS for retrieval
- Answers questions using LLM (Groq)

## How to run
```bash
pip install -r requirements.txt
python main.py